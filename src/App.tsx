import React, {useState} from 'react';
import logo from './logo.svg';
import './App.css';

function App() {
  const [sampleArray, setSampleArray] = useState([1,4,2,3,5]);

  const sorting = (array: number[]) => {
    
  }

  return (
    <div className="App">
      
    </div>
  );
}

export default App;
